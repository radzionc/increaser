import { VStack } from '@lib/ui/layout/Stack'
import styled from 'styled-components'

export const BreakdownContainer = styled(VStack)`
  gap: 16px;
  overflow-x: auto;
  align-items: start;
  font-weight: 500;
`
