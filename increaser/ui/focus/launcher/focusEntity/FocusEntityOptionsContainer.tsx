import { VStack } from '@lib/ui/css/stack'
import { verticalPadding } from '@lib/ui/css/verticalPadding'
import styled from 'styled-components'

export const FocusEntityOptionsContainer = styled(VStack)`
  ${verticalPadding(8)}
`
