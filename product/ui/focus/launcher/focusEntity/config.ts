import { focusIconButtonSize } from '../../FocusSetWidget/FocusIconButton'

const rightPadding = 8

export const focusEntityConfig = {
  height: focusIconButtonSize + rightPadding * 2,
  rightPadding,
}
